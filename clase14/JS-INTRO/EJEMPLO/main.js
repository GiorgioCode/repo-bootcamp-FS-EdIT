/**
 * 1. camelCase - inicia con minuscula y luego cada
 * palabra con mayuscula
 * Ej: perroGordo
 *
 * 2. PascalCase - Igual que camelCase pero iniciando
 * con mayusula
 * Ej: PerroGordo
 *
 * 3. kebab-case - se separan las palabras con un guion
 * medio (simula la espada del kebab)
 * Ej: perro-gordo
 *
 * 4. snake_case - se separa con guion bajo
 * Ej: perro_gordo
 */

// DATOS PRIMITIVOS
let nombreUsuario = "Jorge"; //dato de tipo string
let edadUsuario = 39; //dato tipo number
let usuarioValido = true; //dato tipo boolean
let indefinido = undefined; // dato sin tipo

//DATOS ESTRUCTURADOS
let arreglo = [0, 1, 2, 3, "pepe", true, [0, 1]];
let objeto = {
    nombre: "jorge",
    apellido: "paez",
    edad: 39,
};

